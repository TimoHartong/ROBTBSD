#include "sys/cdefs.h"

#include <sys/ioctl.h>
#include <sys/param.h>
#include <sys/uio.h>
#include <sys/select.h>
#include <sys/fcntl.h>

#include <bluetooth.h>
#include <err.h>
#include <errno.h>
#include <fcntl.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <signal.h>
#include <termios.h>
#include <unistd.h>
#include <util.h>
//#include "sys/bsdfix.h" // Added to have it compiled

/**
* This file contains the dummy and fixes for netbesd
*/

/* Defined in sys/ioctl.h */
int ioctl(int d, unsigned long request, ...)
{

}

/* Defined in unistd.h */
int close(int d)
{

}

ssize_t	read(int d, void *buf, size_t nbytes)
{
}

/* sys/fcntl.h */
int open(const char *path, int flags, ...)
{
}

/* sys/uio.h */
ssize_t	writev(int d, const struct iovec *iov, int iovcnt)
{

}

/* sys/select.h */
int select(int nfds, fd_set *readfds, fd_set *writefds, fd_set *exceptfds, struct timeval *timeout)
{
}

/* unknown */
int sigaction(int sig, const struct sigaction *act, struct sigaction *oact)
{
}

/* sys/sysctl.h */
void *asysctlbyname(const char *sname, size_t *len)
{
}

/* sys/socket.h */
int bind(int s, const struct sockaddr *name, socklen_t nameLen)
{

}

int connect(int s, const struct sockaddr *name, socklen_t nameLen)
{
}

int socket(int domain, int type, int protocol)
{
}

/* time.h */
int nanosleep(const struct timespec *rqtp, struct timespec *rmtp)
{

}

/* err.h */
void warnx(const char *fmt, ...)
{
}

/* util.h */
int snprintb(char *buf, size_t bufsize, const char *fmt, uint64_t val)
{

}

//int pidfile(const char *path)
//{
//}
